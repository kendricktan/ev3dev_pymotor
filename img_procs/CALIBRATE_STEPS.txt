# Steps to calibrate robot to lighting conditions
### settings.py contains all calibration settings
1. Run setup_line_follow_gui_viewonly.py to see if it can detect the green lines + black lines
  > if can't detect black lines
    > edit THRESH till satify needs
  > if can't detect green lines
    > edit GREEN_RANGE till detects it
